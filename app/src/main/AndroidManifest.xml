<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.test.huazi">

    <application
        android:name=".activity.TestApp"
        android:allowBackup="true"
        android:icon="@mipmap/btc_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/btc_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bitcoin_app">
        <activity android:name="com.test.huazi.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>